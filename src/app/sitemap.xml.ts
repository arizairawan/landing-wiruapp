// This file's functionality for generating an XML sitemap at /sitemap.xml
// has been superseded by the HTML page at /src/app/sitemap.xml/page.tsx.
// To generate a standard XML sitemap for search engines, Next.js App Router convention
// is to use a `src/app/sitemap.ts` (or .js) file.
// This current file can be safely removed if no longer needed for other purposes.

export {}; // Ensures this file is treated as a module and doesn't cause errors if imported elsewhere.
